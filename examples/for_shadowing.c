int main() {
    int x = 1;
    int y = 2;
    int result = 0;

    if (x = 10)
        if (y = 20)
            for (int x = 0; x < y; x = x + 1)
                for (int y = 0; y < 10; y = y + 1)
                    while (x == 5)
                        do
                            if (x == y)
                                break;
                            else
                                continue;
                        while (y == 5);

    return x+y;
}