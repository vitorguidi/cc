int foo(int x, int y);
int bar(int z);

int main() {
    int foo(int a, int b);
    if (2>10) {
        return bar(10)*foo(2,3) + bar(20);
    } else {
        return bar(20)*foo(40,80);
    }
}

int bar();

int bar(int x) {
    return 2 == 4;
}

