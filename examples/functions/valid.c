int foo(int x, int y);
int bar(int z);
int main() {
    int foo(int a, int b);
    int bar(int z);
    if (2>10) {
        return bar(10)*foo(2,3) + bar(20);
    } else {
        return bar(20)*foo(40,80);
    }
}
int foo(int x, int y) {
    int bar(int k);
    return bar(x) + 2*y;
}
int bar(int x) {
    int foo(int w, int z);
    return x > 4;
}